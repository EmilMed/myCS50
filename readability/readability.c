#include <cs50.h>
#include <stdio.h>
#include <string.h>
#include <math.h>

int main(void)
{
    string text = get_string("Text: ");
}
int letters = 0
int count_letters(string text)
{
    for (i = 0, i )
}